extends layout

block content
  .row.error-page
    .column
      a(href=DOMAIN)
        img(src="/images/logo.png", alt="Pollermo logo", class="logo")
      h2= error.status
      p= message
      a(href=`${DOMAIN}` className="button is-primary") GO TO HOME
      if !!error
          pre #{error.stack}
      